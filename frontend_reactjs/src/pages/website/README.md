# Website pages (Ocean Professional)

This folder contains the marketing/website scaffold:

- HomePage (`/`)
- AboutPage (`/about`)
- ServicesPage (`/services`)
- PricingPage (`/pricing`)
- ContactPage (`/contact`)

The operational dashboard lives under `/app/*` routes (service requests CRUD).
